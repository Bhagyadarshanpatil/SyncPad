{
  "name": "syncpad",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently \"pnpm -F @syncpad/server dev\" \"pnpm -F @syncpad/client dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "es5-ext",
      "esbuild"
    ]
  }
}
